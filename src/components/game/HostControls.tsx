import React from "react";

const HostControls = ({ gameId }: { gameId?: string }) => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      HostControls
    </div>
  );
};

export default HostControls;
